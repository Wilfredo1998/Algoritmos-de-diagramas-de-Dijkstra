void  (int inicial ){
	int():
		Q. push (nodo(inicial , 0) );
		distancial[ inicial]=0;
		int actual , adycebte , peso;
		
		while (! Q. e mp t y(){
			actual = Q.top().first;
			Q.pop();
			if(visitado)[actual])continue;
			
			for(int i = 0; i < ady [actual].size;**i){
				adyacente = ady [actual][i].first;
				paso = ady [actual][i].second;
				if(!visitado[adyacente]){
				mi ni mazion( actual, adyacente, paso);
				
			
			}
		}
}
